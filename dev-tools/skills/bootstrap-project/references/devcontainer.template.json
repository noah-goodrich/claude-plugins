{
  "name": "PROJECT_NAME",
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspace",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [],
      "settings": {}
    }
  },
  "postCreateCommand": "true",
  "postStartCommand": "sudo mkdir -p /Users && sudo ln -sfn /home/dev /Users/__USERNAME__; sudo chmod a+rw /run/host-services/ssh-auth.sock 2>/dev/null || true",
  "shutdownAction": "stopCompose",
  "remoteUser": "dev",
  "updateRemoteUserUID": true
}
