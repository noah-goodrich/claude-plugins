# 2026-06-10-1141 — claude-plugins checkpoint

## 1. Goal

Review the `directives-02-06` branch, fix what the review found and ship it, then resolve the
borg ↔ cairn ↔ plugin source-of-truth question into a unified plan.

## 2. Accomplished

- **Multi-agent code review** of `directives-02-06` (research-tools hooks) → **15 CONFIRMED findings**
  (fail-open: A3 `>`-form bands, A7 phantom denominator + BSD-sed `\b` no-op + percent literal,
  A10/A12 wrong-integer reads, A9 substring/TLD/scheme gaps, W1 `grep -EI` case typo, A1 `sources/`
  mis-discovery; fail-closed: unquoted `$SCAN_FILES`/`$WARN_SCAN` word-split, A6/A8 substring
  false-positives; plus `stop.sh` exit-2 conflation and `scholarly-adapter.sh` arity crash).
- **Fixed all 15** in `deep-research-verify.sh`, `deep-research-stop.sh`, `scholarly-adapter.sh`.
- **Added `research-tools/hooks/test/run-tests.sh`** — 39-check regression suite (16 fixtures +
  12 evasion/honest mutations + 11 cross-cutting). All pass; **zero regressions**; shellcheck clean;
  validated on macOS bash 3.2.
- **Filed Directive 07** (`07-shared-card-report-parser.md`) — the altitude follow-up (one shared
  parser feeding A2/A3/A7/A8/A10/A12 instead of per-assertion grep/sed scrapes).
- **Committed** all of the above to `directives-02-06` as **`44a3e83`** (5 files, +438/−92).
- Established that the deep-research/brainstorm work is tracked as `docs/brainstorms/2026-06-05-next-gen-
  research-invention.md` → directives **01–07** (no `PROJECT_PLAN.md`); chose the **directive-lifecycle**
  assimilation path (move 01–06 → `assimilated/`, 07 stays pending).
- Ran the **cairn + CLI + plugin investigation workflow** (9 agents, adversarially reconciled). Verified
  current state and produced a 3-layer target architecture, a source-of-truth rule, a corrected
  cairn-write diagnosis, a 9-step migration, and 6 open decisions. Key truths: **no active cairn bug**
  (stale-install log noise + dead `timeout` cap, writes succeed); a **model-less CLI cannot run plugin
  skills** (so "thin CLI" = CLI stops *authoring* portable artifacts, not a shell over the plugin);
  **5 install targets** (Claude `~/.claude/{hooks,lib,skills}`, Cortex/CoCo, plugin); **sub-task →
  cairn-api:8767 reachability is UNVERIFIED** (the load-bearing assumption behind moving the writer).

## 3. Ready to Commit

- Already committed this session: **`44a3e83`** on `directives-02-06`.
- Working tree now: `M .gitignore` (NOT from this session — borg/linter touch) and `?? .borg-project`
  (scaffolding). Neither is a session deliverable; leave for triage.
- `/simplify` already run this session → clean (shellcheck clean, no dead code). Nothing else pending.
- The unified cairn plan is **not yet written to disk** — deliberately held until the forks below are
  decided.

## 4. Blockers — ALL OUTSTANDING QUESTIONS

**Nothing proceeds until these are answered. The user asked to clarify the cairn questions before
deciding (did not answer them yet).**

### A. Ship of PR #8 (deep-research/brainstorm work) — staged, NOT executed
1. **Go/no-go**: merge PR #8 (`directives-02-06` → `main`)? Steps push + assimilate-commit are
   reversible; the merge to `main` is the irreversible one. NOTHING pushed/merged yet.
2. **Merge method**: `--merge` (preserve per-directive history, recommended) vs `--squash`?
3. Note: PR #8 also carries 3 borg-collective sync commits (`notify` inline, `borg-link`/`borg-next`
   align) — they ride to `main` with it. Confirm that's fine.

### B. Cairn unified plan — the 3 reformulated forks (asked, user chose to clarify first)
4. **STEP -1 gate**: (a) I spawn a Cowork sub-task and test `cairn-api:8767` reachability before
   committing to the writer-move [recommended]; (b) skip the writer-move entirely (you already know
   sub-tasks don't reach cairn / don't need sub-task telemetry); (c) assume it works, no test.
5. **Hook scope into the plugin**: (a) container-critical only — writer + bash-guard + nudges, leave
   host-state hooks (link-down SessionStart, plan-promote, nanoprobe) CLI-only [recommended]; (b) all
   lifecycle hooks (bigger `borg-hooks.sh` split).
6. **Your-machine registration channel** (mutually exclusive — both = double-fire Stop): (a) keep
   `borg setup`/settings.json, sourced from the plugin tree, don't install the plugin locally
   [recommended]; (b) plugin-only, and `borg setup` stops registering lifecycle hooks.

### C. Cairn plan — remaining open decisions (from the workflow, not yet posed)
7. **CoCo source after inversion**: `cortex skill add` reads from (i) the plugin SOURCE tree directly,
   or (ii) a retained CLI `skills/` kept in sync by the CI drift check? (Cortex can't load a `.plugin`,
   so one must persist.)
8. **MCP now vs later**: ship the writer-move on the existing `cairn record session` REST shim first
   [recommended], then MCP-first as a separate PR before cairn v0.3 removes REST — or absorb MCP now?
9. **Personal-install source**: `borg setup` copies from the plugin SOURCE tree
   (`~/dev/claude-plugins/borg-collective`, simpler dev loop) [recommended] or the built `dist/*.plugin`?

### D. Clarifications the user signaled wanting (feed B/C above)
10. Do you already KNOW whether Cowork sub-tasks join `devnet`/reach `cairn-api:8767`? (settles Q4)
11. Does the "thin CLI = stops authoring portable artifacts; can't run plugin logic headless" reframe
    match your mental model? (changes the whole layer split if not)
12. Do you actually need sub-task-level cairn telemetry, or is session-level enough? (decides whether
    the writer-move is worth doing at all)

### E. Downstream / related
13. C6 of the marketplace `PROJECT_PLAN.md` (borg-collective source-of-truth) is resolved BY the cairn
    plan above; once that lands, the marketplace plan can be assimilated separately.
14. STEP 0 quick win (independent of all forks): re-run `borg setup` to install the Jun-9 Stop hook
    (stderr capture) and `brew install coreutils` so the 5s cairn-write `timeout` cap actually applies.

## 5. Next Session

1. **Resolve the clarifications (Q10–Q12) first** — they collapse several of the forks. Then settle
   B (Q4–Q6) and C (Q7–Q9).
2. **Write the unified plan to disk** as the resolution of `docs/plans/directives/2026-05-27-borg-cairn-
   coordination.md` (the C6 directive). Full reconciled proposal is in the workflow output:
   `/private/tmp/claude-501/-Users-noah-dev-claude-plugins/fc8d4499-920c-4293-9601-68aee49e7ed3/tasks/w9qcziuvv.output`
   (current_state, 3-layer architecture, source_of_truth_rule, cairn_write_fix, 9 migration steps).
3. **Ship PR #8** per the directive-lifecycle choice: `git push` → assimilation commit (`git mv`
   `research-tools/docs/plans/directives/0{1..6}-*.md` → `assimilated/`, mark criteria `[x]` + Shipped
   stamp, 07 stays) → `gh pr merge 8 --merge` → verify tree clean.
4. **STEP 0 quick win** (can do anytime): `borg setup` + `brew install coreutils` + patch the hook
   timeout guard to prefer `timeout`/`gtimeout`.

Re-run the regression suite before merging: `bash /Users/noah/dev/claude-plugins/research-tools/hooks/test/run-tests.sh`
